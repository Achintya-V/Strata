# Strata — full environment for the library, demo chatbot, and benchmark runner.
# Install:  pip install -r requirements.txt && pip install -e .

# --- strata core (mirrors pyproject.toml [project].dependencies) ---
pyyaml>=6.0
python-frontmatter>=1.1.0
pydantic>=2.5
typer>=0.12.0
rich>=13.0.0
filelock>=3.12

# --- demo chatbot (streamlit UI + Nemotron via NVIDIA's OpenAI-compatible API) ---
streamlit>=1.36
openai>=1.40
python-dotenv>=1.0

# --- tests / dev ---
pytest>=8.0

# --- optional extras (uncomment what you need; mirror pyproject extras) ---
# anthropic>=0.40.0        # [llm]    LLM memory extraction with Claude
# mcp>=1.0.0               # [mcp]    MCP server for Claude Code/Desktop
# sqlite-vec>=0.1.3        # [vector] hybrid vector search
# model2vec>=0.3           # [vector]
# presidio-analyzer>=2.2   # [pii]    stronger PII scanning
# presidio-anonymizer>=2.2 # [pii]
